import 'package:flutter/material.dart';

class CustomInfoIcon extends StatelessWidget {
  const CustomInfoIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
